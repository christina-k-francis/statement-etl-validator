# statement-etl-validator
A python-based pipeline that extracts holdings information from brokerage pdf statements, loads it into a cohesive CSV file, conducts an LLM-assisted iterative validation cycle for data quality assurance, and then loads the data into a PostgreSQL database.
